# Feature request: Refresh README demo and add ASCII art banner to the TUI

## Summary

Two polish items:

1. Prettify the interface by displaying ASCII art spelling **atty** at the top of the TUI.
2. Regenerate the demo GIF (`demo.gif`) referenced in `README.md` so it reflects the current behavior (cache-backed recency sorting).

## Motivation

- A small ASCII art header gives the TUI a distinctive, polished look at essentially zero runtime cost.
- The existing `demo.gif` is stale and no longer shows the newest features, which hurts first impressions for anyone landing on the repository.
