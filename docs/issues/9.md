# Keep `name contains` only as disambiguation fallback

## Summary

Approach #8 so that unique titles follows exact matching flow.

## Proposed behavior

- On raise/close: `name is %q`.

## Acceptance criteria

- Unique exact title: one `is` resolution
